[package]
name = "raft"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2"

[dev-dependencies]
proptest = "1"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
